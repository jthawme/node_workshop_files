// This is a function, it is reusable code.
// It means we dont have to repeat ourselves all the time
function returnRandomNumber() {
    return 'Look at this great number: ' + Math.random();
}

// Console logging the function being ran,
// will log the OUTPUT of the function
console.log(returnRandomNumber());