// Here we are 'requiring' a module into our code.
// It is an external function that lives inside of
// node_modules.
var random_name = require('node-random-name');

console.log(random_name());

// One of the great parts of node.js and javascript
// is the large community of work and modules you 
// can import/require into your own work!